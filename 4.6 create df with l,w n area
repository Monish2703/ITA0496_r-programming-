calculate_area <- function(length = 1, width = 1) {
  return(length * width)
}

df <- data.frame(
  length = c(4, 5, NA, 6, 7),
  width = c(2, NA, 3, 4, 5)
)
df$length[is.na(df$length)] <- 1
df$width[is.na(df$width)] <- 1
df$area <- mapply(calculate_area, df$length, df$width)
